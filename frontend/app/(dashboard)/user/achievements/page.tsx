import Feats from "@/components/dashboard/Feats";

export default function Achievements() {
    return (
        <main className="flex min-h-screen flex-col">
            <Feats />
        </main>
    );
}
